.build/
.build-test/
dist/
build.log
.DS_Store
*.xcuserdatad
*.xcuserstate
.swiftpm/
AppIcon.iconset/
__pycache__/

# Signing material — private keys must never be committed
.signing/

# Release artefacts staged for the site — the DMG is built, not tracked
site/public/*.dmg
site/public/*.zip
