<!--
Review lifecycle:
- Use `Fixes BIV-n` when this PR completes the Linear issue, or `Refs BIV-n`
  when the issue outlives it. After opening, verify the PR appears as an attachment
  on that Linear issue.
- Set the linked issue to `Agent review` only after a real review executor exists:
  either this Conductor workspace owns the native Review action, or a non-Conductor
  producer has dispatched a named independent read-only reviewer against the exact
  head. Otherwise keep the issue `Active`. Do not request Bradley or move it to
  `Review` yet. The owning agent does both only after exact-head review is clean,
  required GitHub checks and threads are clear, and the exact-head handoff succeeds.
-->

## Relationships
<!-- Fixes BIV-n · Refs BIV-n · Relates to BIV-n — or add the `no-issue` label -->

## Summary

## Verification

## Follow-ups
